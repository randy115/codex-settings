interface:
  display_name: "Herdr Jobs"
  short_description: "Run background Codex jobs in Herdr"
  default_prompt: "Use $herdr-jobs to start a background Codex task in the current Herdr workspace."

policy:
  allow_implicit_invocation: false
